digits = range(1,170) 

print(max(digits)) # max function returns the maximum value in the list
print(min(digits)) # prints the minimum and maximum number in the list
print(sum(digits)) # sum of the elements in the list